% =====================================================================
%  metadata.tex -- final overrides for branding & color palette
%  Re-color or rebrand the entire book by editing only this file.
% =====================================================================

% Example: uncomment to swap brand color globally
% \definecolor{rcfeNavy}{HTML}{0F2A44}
% \definecolor{rcfeGold}{HTML}{D4AF37}

% Section accent presets (colors used by \SectionAccent in each section)
% Override here if desired -- otherwise sections use sensible defaults.

\endinput
